// Copy this file to config.js and fill in your values.
// config.js is gitignored and never committed.
// API_SECRET must match the SHARED_SECRET env var on Render
// and the SHARED_SECRET value in whisperx-server/.env.
const API_BASE_URL = 'https://your-render-service.onrender.com';
const API_SECRET   = 'your-secret-here';
