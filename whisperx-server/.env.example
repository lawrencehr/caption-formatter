# WhisperX Server Environment Variables
# Copy this file to .env and update the values

# Shared secret used for API authentication
# Must match the SHARED_SECRET in the Render proxy (server.js)
SHARED_SECRET=your-shared-secret-here

# Server port (default: 8765)
# This is the internal port; Cloudflare Tunnel handles external exposure
PORT=8765
